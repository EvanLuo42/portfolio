---
import {
  Card,
  CardContent
} from "./ui/card";

const { title, className } = Astro.props
---

<div class:list={["col-span-1 xl:col-span-4 md:col-span-3 row-span-1", className]}>
    <h3 class="text-2xl font-bold">{title}</h3>
</div>